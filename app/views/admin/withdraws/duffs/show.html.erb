<div class="row">
  <div class="col-xs-12">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <span><%= t('.withdraw', sn: @duff.id) %></span>
      </div>
      <div class="panel-body">
        <%= description_for :withdraw do %>
          <%= item_for @duff, :id %>
          <%= item_for @duff, :created_at %>
          <%= item_for @duff, :aasm_state_text %>
          <hr class="split">
          <%= item_for @duff.member, :name %>
          <%= item_for @duff, :fund_extra %>
          <%= item_for @duff, :fund_uid do %>
            <span>
              <%= @duff.fund_uid.truncate(22) %>
            </span>
          <% end %>
          <%= item_for @duff, :amount %>
          <hr class="split">
          <%= item_for @duff, :remark %>
          <hr class="split">
          <ul class="list-inline pull-right">
            <% if @duff.may_reject? %>
              <li>
                <%= link_to t('.reject'), url_for([:admin, @duff]), class: 'btn btn-danger', method: 'DELETE', confirm: t('.reject_confirm') %>
              </li>
            <% end %>
            <% if @duff.may_process? %>
              <li>
                <%= link_to t('.process'), url_for([:admin, @duff]), class: 'btn btn-primary', method: 'PATCH' %>
              </li>
            <% end %>
          </ul>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-xs-12">
    <%= render partial: 'shared/admin/member', locals: {member: @duff.member} %>
    <%= render partial: 'shared/admin/account', locals: {account: @duff.account} %>
  </div>
</div>
