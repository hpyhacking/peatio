<div class="row">
  <div class="col-xs-12">
    <div class="panel panel-primary">
      <div class="panel-heading">
        <span><%= t('.withdraw', sn: @litoshi.id) %></span>
      </div>
      <div class="panel-body">
        <%= description_for :withdraw do %>
          <%= item_for @litoshi, :id %>
          <%= item_for @litoshi, :created_at %>
          <%= item_for @litoshi, :aasm_state_text %>
          <hr class="split">
          <%= item_for @litoshi.member, :name %>
          <%= item_for @litoshi, :fund_extra %>
          <%= item_for @litoshi, :fund_uid do %>
            <span><%= @litoshi.fund_uid.truncate(22) %></span>
          <% end %>
          <%= item_for @litoshi, :amount %>
          <hr class="split">
          <%= item_for @litoshi, :remark %>
          <hr class="split">
          <ul class="list-inline pull-right">
            <% if @litoshi.may_reject? %>
              <li>
                <%= link_to t('.reject'), url_for([:admin, @litoshi]), class: 'btn btn-danger', method: 'DELETE', confirm: t('.reject_confirm') %>
              </li>
            <% end %>
            <% if @litoshi.may_process? %>
              <li>
                <%= link_to t('.process'), url_for([:admin, @litoshi]), class: 'btn btn-primary', method: 'PATCH' %>
              </li>
            <% end %>
          </ul>
        <% end %>
      </div>
    </div>
  </div>

  <div class="col-xs-12">
    <%= render partial: 'shared/admin/member', locals: {member: @litoshi.member} %>
    <%= render partial: 'shared/admin/account', locals: {account: @litoshi.account} %>
  </div>
</div>
